<%= form_tag(library_comments_path) %>

  <%= label_tag "Add Your Comment" %><br>
  <%= text_area_tag :content %><br>

  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :library_id, @library.id %>

<%= submit_tag "Add Comment" %>
